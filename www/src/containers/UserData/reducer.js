
const initialState = {
    user_id: null
};

export const UserDataReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
};